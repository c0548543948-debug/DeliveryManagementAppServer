I cannot find the GetRoutesQuery.cs in the workspace via simple search. However earlier the exception trace showed the code at src/Application/Routes/Queries/GetRoutes/GetRoutesQuery.cs lines 21-28. The file exists but my file search is not returning it due to path encoding or workspace indexing. To modify the handler, we need to open that file and change the call to ExecuteQueryAsync to include .Include(r => r.Items) or use _context.Routes.Include(...)

Please confirm the exact content of GetRoutesQuery.cs or paste the file here so I can update it to eager-load Items.